import React from 'react';

const VideoListItem = ({video}) => {
    // const video = props.video; ES6 {video} does the same
    console.log(video)
    return <li>Video</li>
};

export default VideoListItem;